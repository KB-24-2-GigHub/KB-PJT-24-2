# 컨텍스트를 WAR 하나로 줄인다. src/ 와 .gradle/ 이 전송되면 빌드가 느려지고
# 파일이 하나라도 바뀔 때마다 캐시가 깨진다.
#
# 단계별로 되짚어 포함하는 형태여야 한다. `*` 로 전부 제외한 뒤 곧바로
# `!build/libs/gig-hub.war` 를 쓰면 중간 디렉터리가 제외된 상태라 재포함이
# 동작하지 않을 수 있다.
*
!build
build/*
!build/libs
build/libs/*
!build/libs/gig-hub.war
