<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gighub.wallet.mapper.WalletQueryMapper">

    <!-- SQL 열을 Mapper 전용 Row의 불변 생성자에 명시적으로 연결합니다. -->
    <resultMap id="walletSummaryRowResult" type="com.gighub.wallet.mapper.result.WalletSummaryRow">
        <constructor>
            <idArg column="walletId" javaType="java.lang.Long"/>
            <arg column="availableBalance" javaType="java.lang.Long"/>
            <arg column="lockedBalance" javaType="java.lang.Long"/>
        </constructor>
    </resultMap>

    <resultMap id="walletTransactionRowResult" type="com.gighub.wallet.mapper.result.WalletTransactionRow">
        <constructor>
            <idArg column="transactionId" javaType="java.lang.Long"/>
            <arg column="type" javaType="java.lang.String"/>
            <arg column="amount" javaType="java.lang.Long"/>
            <arg column="availableBefore" javaType="java.lang.Long"/>
            <arg column="availableAfter" javaType="java.lang.Long"/>
            <arg column="lockedBefore" javaType="java.lang.Long"/>
            <arg column="lockedAfter" javaType="java.lang.Long"/>
            <arg column="workCaseId" javaType="java.lang.Long"/>
            <arg column="workTitle" javaType="java.lang.String"/>
            <arg column="workplaceName" javaType="java.lang.String"/>
            <arg column="createdAt" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <select id="findWalletSummaryByUserId" resultMap="walletSummaryRowResult">
        SELECT id                AS walletId,
               available_balance AS availableBalance,
               locked_balance    AS lockedBalance
        FROM wallets
        WHERE user_id = #{userId}
    </select>

    <!-- 목록과 카운트가 같은 조건을 쓰도록 공유한다 -->
    <sql id="transactionConditions">
        WHERE w.user_id = #{userId}
        <if test="workplaceId != null">
            AND wc.workplace_id = #{workplaceId}
        </if>
        <if test="from != null">
            AND wt.created_at &gt;= #{from}
        </if>
        <if test="toExclusive != null">
            AND wt.created_at &lt; #{toExclusive}
        </if>
        <if test="type != null">
            AND wt.transaction_type = #{type}
        </if>
        <if test="minAmount != null">
            AND wt.amount &gt;= #{minAmount}
        </if>
        <if test="maxAmount != null">
            AND wt.amount &lt;= #{maxAmount}
        </if>
        <if test="keyword != null">
            AND (
                wc.title LIKE CONCAT('%', #{keyword}, '%')
                OR wp.name LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
    </sql>

    <!-- work_case_id는 NULL 가능(충전·출금)하므로 LEFT JOIN을 사용한다 -->
    <select id="findTransactions" resultMap="walletTransactionRowResult">
        SELECT wt.id               AS transactionId,
        wt.transaction_type AS type,
        wt.amount           AS amount,
        wt.available_before AS availableBefore,
        wt.available_after  AS availableAfter,
        wt.locked_before    AS lockedBefore,
        wt.locked_after     AS lockedAfter,
        wt.work_case_id     AS workCaseId,
        wc.title            AS workTitle,
        wp.name             AS workplaceName,
        wt.created_at       AS createdAt
        FROM wallet_transactions wt
        JOIN wallets w ON w.id = wt.wallet_id
        LEFT JOIN work_cases wc ON wc.id = wt.work_case_id
        LEFT JOIN workplaces wp ON wp.id = wc.workplace_id
        <include refid="transactionConditions"/>
        <!-- 정렬 키는 서버가 매핑한다. 사용자 입력을 SQL에 직접 치환하지 않는다 (API-003) -->
        <choose>
            <when test="sort == 'OLDEST'">ORDER BY wt.created_at ASC, wt.id ASC</when>
            <when test="sort == 'AMOUNT_ASC'">ORDER BY wt.amount ASC, wt.id DESC</when>
            <when test="sort == 'AMOUNT_DESC'">ORDER BY wt.amount DESC, wt.id DESC</when>
            <otherwise>ORDER BY wt.created_at DESC, wt.id DESC</otherwise>
        </choose>
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countTransactions" resultType="long">
        SELECT COUNT(*)
        FROM wallet_transactions wt
        JOIN wallets w ON w.id = wt.wallet_id
        LEFT JOIN work_cases wc ON wc.id = wt.work_case_id
        LEFT JOIN workplaces wp ON wp.id = wc.workplace_id
        <include refid="transactionConditions"/>
    </select>

</mapper>
