<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gighub.contract.mapper.WorkContractMapper">

    <!--
        두 이름을 한 번에 읽습니다. 한쪽이라도 없으면 결과가 비어 호출부가 무결성 문제로
        판단할 수 있습니다.
    -->
    <select id="findPartyNames"
            resultType="com.gighub.contract.mapper.result.ContractPartyNamesRow">
        SELECT employer.name AS employer_name,
               worker.name   AS worker_name
        FROM users employer
                 JOIN users worker ON worker.id = #{workerId}
        WHERE employer.id = #{employerId}
    </select>

    <!--
        조건 값은 근무 행에서 복사한 그대로 저장합니다. 여기서 다시 계산하면 계약서와 근무의
        해석이 갈라질 수 있습니다.

        terms_snapshot 은 이미 직렬화한 JSON 문자열을 그대로 씁니다. MySQL JSON Column이
        문자열을 받아 검증하므로 형식 오류는 저장 시점에 드러납니다.
    -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO work_contracts
            (work_case_id, employer_id, worker_id, title,
             starts_at, ends_at, break_minutes, break_paid,
             workplace_name, workplace_address, workplace_latitude, workplace_longitude,
             allowed_radius_meters, agreed_wage, source_terms_version,
             terms_snapshot, accepted_at)
        VALUES
            (#{workCaseId}, #{employerId}, #{workerId}, #{title},
             #{startsAt}, #{endsAt}, #{breakMinutes}, #{breakPaid},
             #{workplaceName}, #{workplaceAddress}, #{workplaceLatitude}, #{workplaceLongitude},
             #{allowedRadiusMeters}, #{dailyWage}, #{sourceTermsVersion},
             #{termsSnapshotJson}, #{acceptedAt})
    </insert>

</mapper>
