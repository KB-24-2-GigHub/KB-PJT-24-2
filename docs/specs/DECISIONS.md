# 제품 결정 기록

| 항목        | 값              |
| ----------- | --------------- |
| 명세 릴리스 | `7.0.1`         |
| 승인일      | 2026-08-12      |
| 소유자      | PM/Admin Master |

이 문서는 제품 계약에 영향을 주는 승인 결정, 아직 답이 필요한 결정과 폐기된 방향만
기록합니다. 개발 진행률, 작업 목록과 코드 위치는 기록하지 않습니다.

## Approved

| ID                                  | 승인 결정                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 영향 범위                                                                      |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------ |
| DEC-MVP-SCOPE-PRIORITY              | PM/Admin이 2026-08-10 제공한 원본 [MVP_SCOPE.md](MVP_SCOPE.md)의 37개 P0 시나리오를 최상위 제품 목표로 채택한다. 제품 범위·Priority·화면 성공 결과가 다른 계약과 충돌하면 원본이 우선하고, REQUIREMENTS·API_SPEC·DECISIONS·SPEC_TRACEABILITY는 안정적인 ID·공식 계약·추적 정보를 제공한다. 목표 계약의 존재와 현재 구현 완료 판정은 별개이며, P0를 미구현이라는 이유로 P1·Deferred로 내리지 않는다.                                                                                                                                                                                                                                  | MVP P0 시나리오 전체                                                           |
| DEC-WORK-LONG-LIVED-LIFECYCLE       | 근무 생성, 초대 발급, 수락과 계약 확정은 서로 다른 HTTP 요청이다. `DRAFT`·`PENDING`은 장시간 지속 가능한 DB 상태이며 각 요청은 짧은 Transaction을 끝내고 Thread·Connection·Lock을 반환한다. 재접속 뒤 저장 상태로 계속하고 수락 때 초대·조건·Work Case·당사자·OWNER 자금을 다시 검증한 뒤 확정 Aggregate만 원자 Commit한다.                                                                                                                                                                                                                                                                                                                      | WORK-003, WORK-005, INVITE-001, INVITE-004, CONTRACT-001, CONTRACT-002         |
| DEC-MVP-GAP-CLASSIFICATION          | 현재 상태는 코드와 검증 근거로 `Implemented`, `Partial`, `Planned`, `Blocked`를 판정하고 `Refactoring`은 기능 완료 상태가 아닌 구조 변경 Overlay로만 기록한다. 목표 API를 문서화하거나 리팩터링 이슈를 닫은 사실만으로 기능을 Implemented로 올리지 않는다. 기존 기능 이슈가 없는 공백은 PM 승인 없이 새 이슈를 만들거나 구현하지 않는다.                                                                                                                                                                                                                                                                                                         | TRACEABILITY의 MVP P0 구현 감사                                                |
| DEC-MVP-HEALTH-CERT-REQUIREMENT     | P0의 보건증 범위는 OWNER가 Work Case 조건으로 요구 여부를 정하고 WORKER가 초대 수락 전에 이를 확인하는 것까지다. 보건증 파일 업로드·공유·열람은 P1 문서 흐름이며 P0 수락을 임의로 차단하지 않는다.                                                                                                                                                                                                                                                                                                                                                                                                                                               | INVITE-005, DOC-001, DOC-003, DOC-005~DOC-008                                  |
| DEC-MVP-SETTLEMENT-CONSERVATION     | 정상 지급, 지각 분할 정산과 노쇼 환불은 원 예치액을 보존하고 멱등한 한 Transaction과 수정 불가 원장으로 처리한다. 지각은 WORKER 지급액과 OWNER 환불액 합이 원 예치액이어야 하고 노쇼는 WORKER 0원·OWNER 전액 환불이다. 계산식은 `DEC-LATE-DEDUCTION`, 노쇼 승인 경계는 `DEC-NO-SHOW-OWNER-APPROVAL`을 따른다.                                                                                                                                                                                                                                                                                                                                    | SETTLE-002, SETTLE-004, SETTLE-005, SETTLE-006, WALLET-005, WALLET-006         |
| DEC-WAGE-CALCULATION                | 전체 근무 분은 `startsAt`부터 `endsAt`까지의 분 단위 차이다. `breakPaid=false`일 때만 `breakMinutes`를 빼서 유급 분을 구하고 `dailyWage=floor(hourlyWage×paidMinutes÷60)`으로 원 미만을 버린다. 유급 분과 산정 일당은 양수여야 하며 등록·수정·초대·계약·예치·정산은 서버가 계산한 같은 Snapshot을 사용한다.                                                                                                                                                                                                                                                                                                                                      | WORK-003, WORK-005, WORK-008, INVITE-003, CONTRACT-002                         |
| DEC-LATE-DEDUCTION                  | `deductionAmount=floor(hourlyWage×lateMinutes÷60)`, `workerPaidAmount=dailyWage-deductionAmount`, `ownerRefundAmount=deductionAmount`으로 원 미만을 버린다. 세 금액은 음수가 아니어야 하고 지급액과 환불액 합은 원 예치액과 같아야 한다. 화면 예상액과 수동·자동 정산은 같은 서버 계산 결과를 사용한다.                                                                                                                                                                                                                                                                                                                                          | DASH-001, DASH-002, SETTLE-002, SETTLE-003, SETTLE-004, SETTLE-006             |
| DEC-NO-SHOW-OWNER-APPROVAL          | 시작+1시간 경계의 시스템 작업은 성공 출근이 없는 READY를 `NO_SHOW`로만 전이하고 자금을 이동하지 않는다. 해당 OWNER가 별도 멱등 Operation으로 환불을 승인해야 WORKER 0원, OWNER 원 예치액 전액 환불과 `ESCROW_REFUND` 원장을 한 Transaction으로 확정한다.                                                                                                                                                                                                                                                                                                                                                                                         | ATT-005, SETTLE-005, WALLET-005, WALLET-006                                    |
| DEC-AUTH-SESSION                    | 인증은 Spring Security 기반 HttpSession이며 JWT와 Access Token을 사용하지 않는다. 비인증 `GET /api/auth/session`은 `200 {data:{authenticated:false}}`, 다른 보호 API의 인증 없음·만료는 `401 AUTH_REQUIRED`다.                                                                                                                                                                                                                                                                                                                                                                                                                                   | AUTH-004, AUTH-006, AUTH-007                                                   |
| DEC-AUTH-CSRF                       | `GET /api/auth/csrf`는 204로 `XSRF-TOKEN` Cookie를 준비한다. 앱 최초 실행·로그인 성공·로그아웃 성공 뒤 다시 호출하며 로그인·로그아웃 POST도 CSRF를 검증한다. 실패는 `403 FORBIDDEN`이고 상태 변경 요청을 자동 재실행하지 않는다.                                                                                                                                                                                                                                                                                                                                                                                                                 | AUTH-006                                                                       |
| DEC-AUTH-INPUT                      | 정규화 후 검증한다. `loginId`·`email`은 trim 후 소문자로 변환하며 정규화한 값은 필수이고 각각 최대 50자·255자다. 필수 여부와 최대 길이 외에 최소 길이, 허용 문자 조합 또는 추가 이메일 형식 규칙은 강제하지 않는다. `name`은 trim 후 최대 100자, `phone`은 공백·하이픈 제거 후 0으로 시작하는 9~11자리 숫자다. 비밀번호는 변환하지 않고 8~64자이면서 UTF-8 72byte 이하여야 하며 초과 입력을 절단하지 않는다.                                                                                                                                                                                                                                     | AUTH-002, AUTH-003, AUTH-008, AUTH-009, AUTH-011, WORKPLACE-001, WORKPLACE-002 |
| DEC-AUTH-ERRORS                     | 아이디 없음·비밀번호 불일치·비활성 또는 잠금 계정은 `401 AUTH_REQUIRED`, CSRF 실패는 `403 FORBIDDEN`, 중복 가입은 `409 CONFLICT`, 역할 불일치는 `403 ROLE_MISMATCH`, 입력 검증 실패는 `400 VALIDATION_ERROR`로 통일한다.                                                                                                                                                                                                                                                                                                                                                                                                                         | AUTH-001, AUTH-002, AUTH-004, AUTH-006                                         |
| DEC-API-ENVELOPE                    | 단일 성공은 `{data}`, 목록은 `{data:{content,page}}`, 오류는 `{code,message,traceId,fieldErrors?}`로 통일한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | COMMON-001, 모든 REST API                                                      |
| DEC-COMMON-5XX                      | 더 구체적인 승인 오류로 변환되지 않은 예상 밖의 서버 오류는 `500 INTERNAL_ERROR`와 `서버 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.`로 응답하고 `fieldErrors`를 생략한다. 응답과 서버 오류 로그는 같은 `traceId`를 사용하며 내부 예외 메시지, SQL, Stack Trace와 민감 정보는 응답에 노출하지 않는다.                                                                                                                                                                                                                                                                                                                                       | COMMON-001, 모든 REST API                                                      |
| DEC-LOCAL-CORS                      | 로컬 직접 호출 Origin은 `http://localhost:5173` 하나이며 Credential을 허용한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 로컬 API 접근                                                                  |
| DEC-TIME                            | API 시점은 UTC `Instant`, 날짜는 `LocalDate`, DB `DATETIME(6)`은 `Asia/Seoul` 지역 시각으로 사용한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | COMMON-004, 모든 시간 필드                                                     |
| DEC-PAGE                            | 목록 기본값은 `page=0`, `size=20`이며 최대 `size=100`이다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 모든 목록 API                                                                  |
| DEC-PROFILE-IMMUTABLE               | 가입 후 `loginId`, `email`, `name`은 변경할 수 없고 일반 프로필에서는 `phone`만 변경한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | AUTH-008                                                                       |
| DEC-PHONE-STORAGE                   | 전화번호 정책은 서로 독립된 `users.phone`, `workplaces.phone`에만 적용한다. 정규화된 숫자를 평문 저장·반환하고 화면에서만 포맷하며 요청·응답·SQL 바인딩 로그에 남기지 않는다. 실제 개인정보 투입, 외부 공개 배포, 검색·중복 검사 전에는 인증된 암호화를 재검토한다.                                                                                                                                                                                                                                                                                                                                                                              | AUTH-002, AUTH-008, WORKPLACE-001, WORKPLACE-002, CONTACT-001                  |
| DEC-OWNER-ONBOARDING                | M2는 `employer_profiles`를 사용하지 않는다. OWNER 가입은 `users`와 `wallets`만 만들고 Mock 은행계좌도 생성·연결하지 않는다. 사업체·사업장 기준정보는 `workplaces`에 두며 `needsWorkplaceSetup`은 요청 시점에 OWNER의 ACTIVE 사업장이 0개인지 실시간 계산한다.                                                                                                                                                                                                                                                                                                                                                                                    | AUTH-002, AUTH-005, WORKPLACE-001                                              |
| DEC-WORKPLACE-LIST                  | OWNER 사업장 목록은 `ACTIVE`·`INACTIVE`를 반환하고 `DELETED`를 제외하며 관리 Item에서 좌표를 제외한다. WORKER의 보건증 공유 후보 목록은 ACTIVE 사업장과 `ACCEPTED`·`READY` Work Case 관계만 `startsAt, workplaceId` 순으로 반환하고 Item은 `workplaceId`, `workplaceName`, `ownerName`, `startsAt`, `endsAt`만 포함한다. 문서별 만료·중복은 공유 POST가 다시 검증한다.                                                                                                                                                                                                                                  | WORKPLACE-003, WORKPLACE-004, DOC-007                                          |
| DEC-INVITE-LIFECYCLE                | 초대는 사전 지정 WORKER가 없는 Bearer Link다. `expiresAt=startsAt`이며 외부 HMAC-SHA-256 Secret과 초대 ID로 파생한 32byte Base64url 무패딩 Token의 SHA-256 Hash만 DB에 저장한다. 활성 초대 재조회는 같은 Token을 200으로 재현하고, HMAC Key 교체 시 활성 초대의 이전 Key를 종료까지 검증 집합에 둔다. 명시적 재발급은 잠금 아래 기존 `PENDING`을 철회하고 새 초대를 만들며 멱등 Replay를 제공하지 않는다. 응답 유실 시 재발급을 재시도하지 않고 일반 발급으로 현재 Link를 조회한다.                                                                                                                                                              | WORK-005, WORK-006, INVITE-001, INVITE-004                                     |
| DEC-INVITE-LOGIN-BADGE              | 비로그인 초대 웹 접근은 `/worker/login?redirect={encodedInvitationPath}`로 연결하고 원래 경로로 복귀한다. 인증 뒤 Bearer Token을 검증한 WORKER에게만 근무 조건을 노출하고 OWNER 뱃지는 `DEC-TRUST-BADGE-CRITERIA`로 즉시 재계산한다. 1~3단계만 `{badgeType:TRUST_OWNER,level}`로 반환하고 0단계는 `ownerBadge:null`이다. 내부 조건 Version과 저장 Key는 노출하지 않고 수락 시 서버가 다시 검증한다.                                                                                                                                                                                                    | BADGE-003, INVITE-002, INVITE-003                                              |
| DEC-INVITE-ACCEPT                   | 초대 수락은 `/api/invitations/{token}/accept`의 Body 없는 POST이며 서버가 유효한 Bearer Token과 인증 WORKER로 당사자를 결정하고 Token으로 Work Case, 조건 Version과 금액을 도출한다. 수락 Aggregate의 원자성·멱등·전자동의는 관련 승인 결정을 따른다.                                                                                                                                                                                                                                                                                                                                                                                            | CONTRACT-001, CONTRACT-002                                                     |
| DEC-INVITE-ERROR-CATALOG            | 형식 오류·미존재 Token은 `404 RESOURCE_NOT_FOUND`, 만료는 `410 INVITATION_EXPIRED`, 철회는 `409 INVITATION_REVOKED`, 수락 완료는 `409 INVITATION_ALREADY_ACCEPTED`, 조건 Version 불일치는 `409 INVITATION_TERMS_CHANGED`다. 역할 불일치는 `403 ROLE_MISMATCH`, 발급 불가 Work Case는 `409 WORK_CASE_LOCKED`, 그 밖의 발급·재발급 상태 충돌은 `409 CONFLICT`다.                                                                                                                                                                                                                                                                                   | INVITE-001, INVITE-002, INVITE-003, INVITE-004, CONTRACT-001                   |
| DEC-WORK-CASE-RESPONSE-SHAPES       | Work Case 요약은 8개 lower camel case Count를 모두 반환한다. 목록 Item과 상세는 승인된 닫힌 필드 집합을 사용하고 별칭·Capability를 추가하지 않는다. `workDate`는 `startsAt`의 `Asia/Seoul` `LocalDate`, 시점은 UTC `Instant`, 금액은 KRW 정수다. 목록·상세 `worker`는 같은 Shape이며 미매칭이면 `null`이다. 최신 초대·계약·에스크로·정산은 Aggregate가 없으면 `null`, `attendance`는 항상 객체이고 성공 기록이 없는 시점만 `null`이다. 화면은 상태·매칭·시각·초대로 동작 가시성을 파생하고 서버가 최종 검증한다.                                                                                                                                 | WORK-001, WORK-002, WORK-003, WORK-004, WORK-005, WORK-006                     |
| DEC-WORKPLACE-RADIUS                | 사업장 인증 반경은 입력·수정할 수 없는 100m 고정값이다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | WORKPLACE-001, ATT-003                                                         |
| DEC-WORKPLACE-IMMUTABLE             | 사업장 대표자, 좌표와 인증 반경은 등록 후 사용자가 직접 수정할 수 없다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | WORKPLACE-002                                                                  |
| DEC-DASHBOARD-REFRESH               | 확보 안심금액은 화면 진입 즉시 계산하고 근무 종료 전까지 60초마다 갱신한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | DASH-002                                                                       |
| DEC-DAILY-WORKER-TAX                | 예상 실수령액은 15만원 초과 과세표준에 소득세 2.7%, 지방소득세 0.27%를 적용하고 각 10원 미만 절사 및 소액부징수 규칙을 적용한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | DASH-003                                                                       |
| DEC-MOCK-ACCOUNT-FIXTURE            | Mock 은행계좌는 사용자보다 먼저 생성한 Demo/Disposable 합성 Fixture이며 Gig Hub 사용자에게 소유·귀속·연결하지 않는다. 가입·프로필·별도 제품 API에서 생성·등록·연결하지 않고 사용자용 목록·CRUD API를 제공하지 않는다.                                                                                                                                                                                                                                                                                                                                                                                                                            | AUTH-002, BANK-001                                                             |
| DEC-MOCK-ACCOUNT-SCHEMA             | 최소 호환선 `V202608051337__replace_mock_bank_account_user_with_pin.sql`에서 `fk_mock_bank_accounts_user`, `idx_mock_bank_accounts_user_status`, `mock_bank_accounts.user_id`를 제거하고 `pin CHAR(4) CHARACTER SET ascii COLLATE ascii_bin NOT NULL DEFAULT '0000'`과 숫자 4자리 `ck_mock_bank_accounts_pin`을 추가한다. 기존 계좌 ID·식별·금액·상태 컬럼과 `funding_orders.linked_account_id`, `withdrawal_requests.linked_account_id`, `mock_bank_transactions.account_id` 참조는 유지한다.                                                                                                                                                   | BANK-001, WALLET-002, WALLET-003                                               |
| DEC-BANK-INPUT                      | 사용자는 canonical `bankCode`와 `accountNo`를 직접 입력하고 서버가 ACTIVE 비귀속 Mock 계좌의 내부 ID를 찾는다. 인증 사용자는 계좌 소유 판정 조건이 아니며 클라이언트가 `bankAccountId`를 보내거나 응답으로 받지 않는다.                                                                                                                                                                                                                                                                                                                                                                                                                          | BANK-001, WALLET-002, WALLET-003                                               |
| DEC-FUNDING-PIN                     | OWNER 충전은 `{bankCode, accountNo, pin, amount}`로 ACTIVE 계좌와 4자리 ASCII 숫자 PIN을 확인한다. Demo PIN은 `0000`이며 실행 시 인증 기준 값은 `mock_bank_accounts.pin`에만 둔다. 요청 PIN은 인증 뒤 폐기하고 주문·원장·멱등 Fingerprint·응답 Snapshot·응답·예외 메시지·오류·감사·SQL 바인딩을 포함한 모든 로그에 원문이나 파생값을 저장하지 않는다. 완료 Replay는 새 이체가 아니므로 PIN을 재검증하지 않는다.                                                                                                                                                                                                                                  | WALLET-002, WALLET-006                                                         |
| DEC-WITHDRAWAL-DESTINATION          | 출금은 `{bankCode, accountNo, amount}`로 ACTIVE Mock 계좌를 입금 대상으로 찾는다. 인증 사용자의 지갑을 차감하므로 계좌 소유권과 PIN을 검사하지 않으며 서버가 찾은 내부 ID를 기존 출금 참조에 기록한다.                                                                                                                                                                                                                                                                                                                                                                                                                                           | WALLET-003                                                                     |
| DEC-BANK-CODE-TABLE                 | 지원 은행 코드는 `004`(KB국민은행), `088`(신한은행), `020`(우리은행), `081`(하나은행), `011`(NH농협은행), `003`(기업은행), `090`(카카오뱅크), `092`(토스뱅크), `089`(케이뱅크), `032`(부산은행), `031`(DGB대구은행), `131`(iM뱅크), `034`(광주은행), `023`(SC제일은행), `027`(씨티은행), `002`(KDB산업은행), `007`(수협은행), `045`(새마을금고), `048`(신협), `071`(우체국) 20개다. `131`은 DGB대구은행과 별도 선택지인 iM뱅크를 위한 프로젝트 전용 canonical 코드이며, 코드표는 합성 Mock 계좌 시연 계약이다. 은행명 별칭은 API 값으로 허용하지 않는다.                                                                                         | BANK-001, WALLET-002, WALLET-003                                               |
| DEC-BANK-INPUT-VALIDATION           | `accountNo`는 공백·하이픈 제거 후 10~14자리 숫자, `amount`는 0보다 크고 100,000,000 이하인 KRW 정수다. 충전 `pin`은 trim·정규화하지 않은 정확히 4자리 ASCII 숫자다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | BANK-001, WALLET-002, WALLET-003                                               |
| DEC-BANK-ERROR-CATALOG              | 입력 형식은 `400 VALIDATION_ERROR`, 충전 계좌 미존재·비활성·PIN 불일치와 출금 계좌 미존재·비활성은 모두 `403 FORBIDDEN` 및 `계좌를 사용할 수 없습니다.`, 계좌·지갑 잔액 부족과 동시 갱신 충돌은 `409 CONFLICT`다. 계좌 승인 실패는 `fieldErrors` 없이 같은 Code·상태·메시지로 응답해 존재·상태·PIN 일치 여부를 구분할 수 없게 한다.                                                                                                                                                                                                                                                                                                              | BANK-001, WALLET-002, WALLET-003, COMMON-001                                   |
| DEC-BALANCE-REFETCH                 | 충전·출금 응답에 최신 잔액을 합치지 않고 성공 후 잔액을 다시 조회한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | WALLET-002, WALLET-003                                                         |
| DEC-IDEMPOTENCY                     | 충전, 출금, 초대 수락, 정상 정산 승인, NO_SHOW 환불 승인과 출퇴근 스캔은 `Idempotency-Key`로 같은 요청을 한 번만 반영한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | WALLET-006, ATT-002, SETTLE-002, SETTLE-005                                   |
| DEC-IDEMPOTENCY-STORAGE             | Key는 `(인증 사용자, Operation, Key)` 단위이며 검증을 통과한 정규화 값으로 Fingerprint를 만든다. 충전·출금은 `bankCode`, `accountNo`, `amount`만 포함한다. 초대 수락의 Operation은 `INVITATION_ACCEPT`이고 Token Hash 소문자 Hex와 `expected_terms_version`으로 `SHA-256(UTF-8("INVITATION_ACCEPT\n" + tokenHashHex + "\n" + termsVersion))`를 만든다. 정상 지급은 `SETTLEMENT_APPROVE`와 `SHA-256(UTF-8("SETTLEMENT_APPROVE\n" + decimalWorkCaseId))`, NO_SHOW 환불은 `SETTLEMENT_NO_SHOW_REFUND_APPROVE`와 `SHA-256(UTF-8("SETTLEMENT_NO_SHOW_REFUND_APPROVE\n" + decimalWorkCaseId))`를 사용한다. PIN·Token 원문과 Fingerprint 입력은 저장·로그하지 않는다. 검증·인증 실패는 저장하지 않고 성공 결과는 24시간 보존하며 완료 Replay는 현재 도메인 상태와 무관하게 저장한 200을 반환한다. | WALLET-006, CONTRACT-002, SETTLE-002, SETTLE-005                               |
| DEC-IDEMPOTENCY-CLAIM-LIFECYCLE     | 멱등 처리는 별도 짧은 트랜잭션에서 `PROCESSING` Claim을 선점한다. 같은 Fingerprint의 `COMPLETED`는 현재 상태를 재검증하지 않고 저장한 200을 Replay하고, `PROCESSING`은 본 처리 잠금을 기다리지 않고 `409 CONFLICT`, 다른 Fingerprint는 `409 IDEMPOTENCY_KEY_REUSED`다. 초대 수락은 구조·인증·역할·빈 Body·Key 형식·Token 존재 검증 뒤 Claim을 만들고 성공 Body와 상태를 Aggregate Transaction의 마지막 DB 변경으로 `COMPLETED`에 원자 Commit한다. 본 처리 실패 Claim은 삭제하며 중단·만료 Claim은 공통 정리 규칙을 따른다.                                                                                                                       | WALLET-006, CONTRACT-002, COMMON-001                                           |
| DEC-TRANSACTION-DISPLAY             | 거래 Item의 `amount`는 절대값이고 `direction`(`CREDIT`/`DEBIT`)이 해당 사용자 지갑의 증감을 나타낸다. 사업장과 무관한 거래는 근무·사업장 필드가 `null`이고 `workplaceId` 필터에서 제외된다. `minAmount`·`maxAmount`와 금액 정렬은 절대값 기준이며 `displayStatus`는 원장 반영 시점에 따라 파생한다.                                                                                                                                                                                                                                                                                                                                              | WALLET-004                                                                     |
| DEC-SETTLEMENT-TIME                 | 정산 완료 시점의 외부 필드명은 `completedAt`이며 UTC `Instant`다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | SETTLE-002                                                                     |
| DEC-SETTLEMENT-LIFECYCLE            | 수락은 합의 일급의 `WAITING/due_at=null`을 만들고 정상·확인된 CHECK_OUT은 Work Case `COMPLETED`, Settlement `SCHEDULED/due_at=recordedAt+24시간`을 한 Transaction에서 만든다. 정산은 Work Case를 변경하지 않는다. OWNER는 Scheduler 선점 전까지 `due_at` 전후 모두 승인할 수 있고 `due_at<=NOW(6)`부터 Scheduler도 자격을 얻는다. 정상 정산은 `WAITING→SCHEDULED`이며 열린 분쟁이 생기면 `SCHEDULED→ON_HOLD`, 마지막 열린 분쟁이 닫히면 `ON_HOLD→SCHEDULED`로 돌아간다. 자금 이동은 `SCHEDULED→PROCESSING→COMPLETED`에서만 허용한다. NO_SHOW 환불은 `WAITING→PROCESSING→REFUNDED`, 자동 지급 실패는 `FAILED`로 끝난다. `PROCESSING`은 자금 Transaction과 따로 Commit하지 않고 `CHECK_OUT_MISSING`은 추가 결정 전 `WAITING/due_at=null`을 유지한다. 현재 Flyway `202608111744`에서도 기존 범용 Claim과 `(status,due_at)` Index를 재사용한다. #72가 현재 지급 동작을 이 상태 전이에 맞춘 뒤 #171 신규 immutable Migration이 `REFUNDED`, 재시도 필드, `disputes.title`과 안전한 상태·시각 제약을 추가한다. 기존 행은 상태별 count로 먼저 감사하며 `COMPLETED+due_at null`, 고착 `PROCESSING`, 금액·원장 모순은 추정 Backfill하지 않고 격리한다. | SETTLE-001, SETTLE-002, SETTLE-003, SETTLE-004, SETTLE-005, SETTLE-006       |
| DEC-SETTLEMENT-IDEMPOTENCY          | OWNER 정상 지급과 NO_SHOW 환불은 기존 Claim의 정확한 24시간 Replay를 사용한다. 같은 Key·Fingerprint 완료는 현재 상태와 무관하게 저장한 200과 Replay Header, 처리 중은 `409 CONFLICT`, 다른 Fingerprint 재사용은 `409 IDEMPOTENCY_KEY_REUSED`이며 본 처리 실패 Claim은 제거한다. 외부 Key 원문을 원장 Key로 쓰지 않고 Settlement ID와 `SETTLEMENT_RELEASE_OWNER`, `SETTLEMENT_RELEASE_WORKER`, `SETTLEMENT_REFUND_OWNER` Namespace의 SHA-256으로 결정한다. 수동 승인과 Scheduler는 같은 정상 지급 Key·원자 실행기를 사용하고 다른 Key 또는 자동 실행 패자는 `SETTLEMENT_ALREADY_PROCESSED`다. | SETTLE-002, SETTLE-003, SETTLE-004, SETTLE-005, SETTLE-006, WALLET-005, WALLET-006       |
| DEC-SETTLEMENT-SCHEDULER            | 자동 지급은 외부 실행 Endpoint와 임의 OWNER 승인자를 만들지 않고 `approved_by_user_id=null`, Settlement ID 기반 `SETTLEMENT_SCHEDULED_PAYOUT`으로 식별한다. `SCHEDULED`, `due_at<=NOW(6)`, 열린 분쟁 없음, 재시도 시각 도달 후보를 `due_at,id` 순으로 최대 100건 조회하고 각 짧은 Transaction에서 `FOR UPDATE SKIP LOCKED`로 선점한다. Deadlock·Lock Timeout·일시 Adapter 실패는 돈·원장을 Rollback하고 1분·5분·15분·60분 Backoff로 최대 다섯 번 시도한 뒤 무자금 이동을 재검증하고 `FAILED`로 격리한다. 무결성 실패도 즉시 `FAILED`이며 고착 `PROCESSING`은 자동 복구하지 않고 수동 대사한다. | SETTLE-003, SETTLE-004                                                         |
| DEC-NO-SHOW-SETTLEMENT              | 성공 CHECK_IN이 없는 `NO_SHOW`, Settlement `WAITING/due_at=null`, Escrow `HELD`만 해당 OWNER가 `SETTLEMENT_NO_SHOW_REFUND_APPROVE`로 승인한다. 자동 환불은 없고 열린 분쟁은 승인을 막는다. 성공은 Settlement `REFUNDED`, Escrow `REFUNDED`, OWNER locked 감소·available 전액 증가와 `ESCROW_REFUND` 한 건을 한 Transaction에서 확정하며 WORKER Wallet·원장은 바꾸지 않는다. | SETTLE-005, WALLET-005, WALLET-006                                             |
| DEC-SETTLEMENT-ERROR-CATALOG        | 정산·분쟁은 인증 없음 `401 AUTH_REQUIRED`, 역할 불일치 `403 ROLE_MISMATCH`, 없는·타 OWNER Work Case `404 RESOURCE_NOT_FOUND`, 열린 분쟁 `409 SETTLEMENT_ON_HOLD`, 승인 불가 상태 `409 SETTLEMENT_NOT_READY`, 이미 처리 `409 SETTLEMENT_ALREADY_PROCESSED`, 처리 중 같은 Key `409 CONFLICT`, 다른 Fingerprint `409 IDEMPOTENCY_KEY_REUSED`, 열린 분쟁 중복 `409 DISPUTE_ALREADY_OPEN`, 제한 재시도 소진 `503 SETTLEMENT_TEMPORARILY_UNAVAILABLE`, 무결성 모순 `500 INTERNAL_ERROR`를 사용한다. | SETTLE-002, SETTLE-003, SETTLE-005, DISPUTE-001, COMMON-001                  |
| DEC-QR-FIXED                        | 사업장 등록 성공 시 OWNER를 발급자로 같은 트랜잭션에서 비만료 활성 고정 QR 한 건을 발급한다. Token은 `v1`, 1~16자 Key ID, 사업장 ID, 16byte nonce, 앞 네 세그먼트의 HMAC-SHA256 다섯 세그먼트이며 binary 값은 무패딩 Base64 URL-safe로 인코딩한다. Key는 외부 필수 설정으로 주입하고 Key ID로 교체하며 DB에는 nonce만 저장하고 완성 Token과 Key를 저장·로그하지 않는다. 조회는 생성·교체 없이 같은 Token을 재현한다. 재발급은 `workplaces → qr_tokens` 순서로 잠그고 기존 활성 QR을 폐기한 뒤 새 nonce를 발급하며 기존 활성 QR이 없어도 복구한다. 서명 검증과 폐기 nonce 상태 거절은 분리하고 같은 QR의 첫 성공 스캔은 출근, 두 번째는 퇴근이다. | WORKPLACE-001, ATT-001, ATT-002                                                |
| DEC-QR-ERROR-CATALOG                | 고정 QR 조회·재발급에서 인증 없음은 `401 AUTH_REQUIRED`, OWNER 역할 불일치는 `403 ROLE_MISMATCH`, 없는 사업장과 다른 OWNER의 사업장은 구분 없이 `404 RESOURCE_NOT_FOUND`, `ACTIVE` 사업장의 활성 QR 누락 조회는 `500 INTERNAL_ERROR`, 활성 QR 유일성 충돌은 `409 CONFLICT`다.                                                                                                                                                                                                                                                                                                                                                                    | ATT-001, COMMON-001                                                            |
| DEC-WORKPLACE-ATTENDANCE-LOCATION   | 현장 브라우저가 GPS 좌표를 공급하고 OWNER가 등록 또는 별도 위치 확정으로 명시 확인하며 서버는 주소로 좌표를 추정하지 않는다. 좌표 없는 ACTIVE 사업장은 한 번만 확정하고 같은 좌표 Replay만 204로 허용한다. 확정 뒤 좌표·반경·주소를 잠그고 목록에는 원문 대신 `attendanceLocationConfirmed`를 반환한다.                                                                                                                                                                                                                                                                                                                                          | WORKPLACE-001, WORKPLACE-002, WORKPLACE-004, ATT-003                           |
| DEC-QR-REISSUE-RECOVERY             | QR 재발급은 `Idempotency-Key`를 요구하지 않는다. 응답 유실은 자동 POST 반복이 아니라 GET으로 현재 QR을 복구하고 사용자가 다시 확인한 재발급만 새 회전 의도다. 재발급과 스캔은 사업장을 먼저 잠그며 선행 Transaction이 승리하고 이미 성공한 근태는 되돌리지 않는다.                                                                                                                                                                                                                                                                                                                                                                               | ATT-001, ATT-002                                                               |
| DEC-ATTENDANCE-LIFECYCLE            | 완전한 수락·계약·SIGNED 문서·HELD·WAITING Settlement·현재 사업장 좌표 Aggregate만 시작 30분 전부터 READY가 된다. 시작 1시간 경계의 성공 출근 없는 READY는 NO_SHOW, 종료 2시간 경계의 성공 출근 있고 성공 퇴근 없는 IN_PROGRESS는 CHECK_OUT_MISSING이다. 스캔과 Scheduler는 Work Case 잠금으로 한 결과만 Commit하며 준비 실패 ACCEPTED는 NO_SHOW가 아니다. 두 종료 상태의 늦은 QR·수동 보정과 자금 이동은 M5에서 제공하지 않는다.                                                                                                                                                                                                                 | ATT-005, ATT-006, SETTLE-001                                                   |
| DEC-ATTENDANCE-IDEMPOTENCY          | 스캔 Scope는 `(인증 WORKER, ATTENDANCE_SCAN, Key)`다. QR Token Hash, 정규화 위도·경도·정확도, UTC 측정 시각과 조기 확인 Boolean의 SHA-256 Fingerprint로 성공·확인 필요 200을 24시간 Replay한다. 실제 CHECK_OUT과 조기 확인은 새 Key다. 본 처리는 `workplaces → qr_tokens → work_cases` 순서로 잠그며 다른 Key 경쟁 패자와 성공 슬롯 Unique 충돌은 `ATTENDANCE_STATE_CONFLICT`, Deadlock·Timeout은 총 3회 뒤 503이다.                                                                                                                                                                                                                             | ATT-002, ATT-004, COMMON-001                                                   |
| DEC-ATTENDANCE-LOCATION-AUDIT       | 스캔은 현재 사업장 좌표, 지구 반지름 6,371,000m의 Haversine과 반올림 전 100m 포함 경계를 사용한다. 브라우저 좌표는 메모리 판정 뒤 보존하지 않고 거리·정확도·측정·시도 시각만 근태 감사에 남긴다. 정확한 후보를 정한 의미상 거부만 승인 failure reason의 REJECTED 행으로 기록하고 QR 변조·후보 없음·복수는 민감값 없는 보안 로그와 `traceId`만 남긴다.                                                                                                                                                                                                                                                                                            | ATT-003, ATT-004                                                               |
| DEC-EARLY-CHECKOUT                  | 예정 종료 전 CHECK_OUT은 미확인 요청에서 근태 행과 상태 변경 없이 `CONFIRMATION_REQUIRED`를 반환한다. 확인은 같은 QR·위치 계약을 다시 검증하는 `confirmEarlyCheckout=true`의 새 Key 의도이며 성공 시 확인 시각을 남긴다. 정상·확인된 퇴근은 COMPLETED와 Settlement `SCHEDULED/due_at=recordedAt+24시간`을 함께 만든다.                                                                                                                                                                                                                                                                                                                           | ATT-004, SETTLE-001                                                            |
| DEC-CHECK-OUT-MISSING               | 성공 출근 뒤 종료 2시간까지 성공 퇴근이 없는 배정 근무만 `CHECK_OUT_MISSING`이며 `NO_SHOW`와 구분한다. M5는 늦은 QR과 수동 보정을 제공하지 않고 Settlement를 `WAITING/due_at=null`로 유지하며 해소·지급·환불은 후속 계약이 담당한다.                                                                                                                                                                                                                                                                                                                                                                                                             | WORK-001, WORK-007, ATT-005, ATT-006, SETTLE-001                               |
| DEC-ATTENDANCE-ERROR-CATALOG        | 스캔은 `QR_INVALID` 422, `QR_REVOKED` 410, `WORKPLACE_LOCATION_REQUIRED` 409, `LOCATION_INVALID`·`OUTSIDE_WORKPLACE_RADIUS` 422, `ATTENDANCE_WORK_CASE_NOT_FOUND` 404, `ATTENDANCE_WORK_CASE_AMBIGUOUS`·`ATTENDANCE_ALREADY_COMPLETED`·`ATTENDANCE_STATE_CONFLICT` 409, `ATTENDANCE_TEMPORARILY_UNAVAILABLE` 503을 사용한다. 공통 입력·멱등 오류는 `VALIDATION_ERROR`, `CONFLICT`, `IDEMPOTENCY_KEY_REUSED`를 유지한다.                                                                                                                                                                                                                          | ATT-002, ATT-003, ATT-004, COMMON-001                                          |
| DEC-ATTENDANCE-RESPONSE-SHAPES      | RECORDED는 근무·유형·기록 시각, 파생 지각, 조기 확인과 정산 예정 시각을 항상 반환한다. CONFIRMATION_REQUIRED는 근무·CHECK_OUT·예정 종료 시각을 반환한다. WORKER 홈·이력은 저장 상태, nullable 성공 근태, 파생 지각, 일급 기반 `expectedNetAmount`와 에스크로·정산을 제공한다. 시급 Snapshot 전에는 `hourlyWage`, `expectedDeductionAmount`, `expectedPaymentAmount`를 제외하고 화면 별칭·정밀 좌표·QR·OWNER 잔액·Storage 정보를 노출하지 않는다. 상태·근태 모순은 저장값을 반환하되 내부 무결성 신호를 남긴다. | DASH-001, DASH-003, WORK-007, ATT-004                                          |
| DEC-ATTENDANCE-BROWSER              | MVP 스캔은 Secure Context에서 Camera, Geolocation, `BarcodeDetector`와 `qr_code` 형식을 모두 지원하는 환경만 허용한다. 미지원·권한 거부는 Token 직접 입력 없이 안내하고 Decoder 의존성 추가는 별도 승인한다.                                                                                                                                                                                                                                                                                                                                                                                                                                     | ATT-002, ATT-003                                                               |
| DEC-ATTENDANCE-SCHEMA               | 현재 좌표·근태·Settlement·범용 Claim Column과 Index로 M5 근태 기능 정합성을 충족하므로 M5 근태 전용 Migration은 추가하지 않는다. 운영 MySQL `EXPLAIN`에서 성능 문제가 확인될 때만 별도 관리자 승인 Index를 검토한다. M6 7.0.0 Settlement Schema는 이 결정과 별개이며 #72 지급 동작 정합화 뒤 #171 신규 immutable Migration으로 적용한다.                                                                                                                                                                                                                                                                                                  | ATT-005, ATT-006, SETTLE-001                                                   |
| DEC-INVITATION-ACCEPTANCE-AGGREGATE | 새 수락 Claim의 본 처리는 Claim → Work Case → Invitation → OWNER Wallet 순서로 잠근다. 초대 상태·만료·조건 Version, Work Case의 `DRAFT`·미매칭·시작 전 상태, 자기 고용 금지와 OWNER 가용 잔액을 검증한다. 성공 시 Work Case·Invitation을 `ACCEPTED`로 전이하고 계약·문서·예치·원장·Settlement와 성공 Claim을 한 Transaction으로 Commit한다. 동시 수락은 Work Case 잠금으로 직렬화되어 첫 Commit만 성공하고 패자는 `INVITATION_ALREADY_ACCEPTED`를 받으며 자금·계약·문서를 만들지 않는다.                                                                                                                                                         | CONTRACT-002, SETTLE-001                                                       |
| DEC-CONTRACT-AUTO-GENERATION        | 근로계약서는 수락 Aggregate가 Work Case당 한 건 자동 생성한다. OWNER가 작성자·소유자이고 WORKER는 `CONTRACT_PARTY` 공유를 받는다. `work_contracts` Snapshot으로 ORIGINAL PDF Version 1과 WORKER `TYPED_NAME` 증거를 포함한 SIGNED Version 2를 만들며 사용자의 직접 업로드·스캔 교체·삭제를 제공하지 않는다.                                                                                                                                                                                                                                                                                                                                      | CONTRACT-003, DOC-002, DOC-010                                                 |
| DEC-E-SIGN-EVIDENCE                 | Body 없는 POST 자체가 WORKER의 최종 동의다. 서버가 잠금 검증 뒤 하나의 수락 시각을 정해 `document_signatures`에 WORKER를 signer, 당시 `users.name`을 `typed_name`, `TYPED_NAME`을 방식, 같은 시각을 `consented_at`·`signed_at`으로 기록한다. Source는 ORIGINAL Version 1, Signed는 SIGNED Version 2이고 두 Checksum은 각 Version과 일치한다. OWNER는 문서 작성자·소유자지만 별도 서명 행을 만들지 않는다.                                                                                                                                                                                                                                        | CONTRACT-001, CONTRACT-003, DOC-004                                            |
| DEC-CONTRACT-FILE-COMMIT            | 계약서 ORIGINAL·SIGNED PDF와 보건증 ORIGINAL은 DB Commit 전에 비공개 저장소의 결정적 임시 Key에 쓰고 DB에는 최종 Key를 기록하며 Commit 뒤 멱등 승격한다. Rollback 시 임시 Object를 최선 노력으로 지우고 고아를 정리한다. 계약 승격 실패는 이미 Commit된 수락 200을 되돌리지 않으며 조회가 최종 SHA-256을 먼저 검증한 뒤 같은 Version의 임시 Object로 Fallback·재승격한다. 둘 다 DB Checksum과 다르면 Stream하지 않는다. 파일·상세는 접근 감사 Commit 뒤에만 응답하며 감사 실패 시 성공 Metadata·파일 Header·Bytes 없이 공통 `500 INTERNAL_ERROR` 오류 Envelope를 반환한다.                                                                                                            | CONTRACT-002, CONTRACT-003, DOC-002, DOC-004, DOC-005, DOC-009~DOC-011        |
| DEC-CONTRACT-RETENTION              | 근로계약서는 사용자가 삭제할 수 없다. `work_cases.ends_at`의 서울 종료 날짜에 3년을 더한 자정부터 매일 02:00 Job이 `documentId` Keyset 100건씩 문서를 먼저 `DELETED`로 Commit한 뒤 모든 Version의 최종·임시 Object를 멱등 삭제한다. 실패한 DELETED 문서는 다음 실행에서 다시 선택한다. `work_case_id` 결손은 데이터 손상으로 격리·경보하고, 문서·Version Metadata·Checksum·서명·공유·접근 감사·계약 관계 행은 기간 제한 없이 보존하며 별도 purge 이력이나 `updated_at` 완료 표시는 두지 않는다. 운영 로그는 traceId·문서/Version ID·단계·결과 Enum만 허용하고 Key·Checksum·당사자 정보는 금지한다. | DOC-012, COMMON-003                                                            |
| DEC-DOCUMENT-STORAGE                | 문서 파일은 외부 비공개 저장소에 두고 식별 Key, Checksum, Version과 접근 감사를 보존한다. 계약 PDF 최종 Key는 `contracts/{workCaseId}/{documentId}/v{versionNo}.pdf`, 임시는 같은 경로의 `.pending/v{versionNo}.pdf`다. 보건증 최종 Key는 `health-certificates/{ownerUserId}/{documentId}/v1.{ext}`, 임시는 같은 경로의 `.pending/v1.{ext}`이며 ORIGINAL Version 1만 둔다. 모든 Checksum은 SHA-256 32byte이고 API·일반 로그에 Key·Checksum을 노출하지 않는다. 보건증은 검증된 JPG·PNG·PDF만, 계약서는 서버 내장 Template과 허용 License 한글 Font의 `application/pdf`만 사용하며 기존 최종 Object는 DB Checksum이 같을 때만 완료다. | CONTRACT-003, DOC-005, DOC-009, DOC-010, DOC-011                              |
| DEC-DOCUMENT-RESPONSE-SHAPES        | 목록과 상세는 `documentId`, `docType`, 계산 `status`, 허용 Version의 `fileName/mimeType/latestVersion`, `issuedDate/expiresDate`, 계산 `source`, 공유자·사업장·Work Case 관계, 닫힌 `capabilities`, `createdAt`을 같은 Item으로 사용하고 상세만 안전한 `versions[]`를 추가한다. OWN은 문서당 한 행, 공유 보건증은 Work Case당 한 행이다. SHARED 보건증 상세는 목록의 `workCaseId` Query가 필수이고 서버가 관계를 자동 선택하지 않는다. 만료 보건증은 소유자에게만 EXPIRED로 보이고 계약 ORIGINAL·전자동의·Storage Key·Checksum·내부 ID는 노출하지 않는다. | DOC-001, DOC-003, DOC-004, DOC-009                                             |
| DEC-DOCUMENT-ERROR-CATALOG          | 문서 API는 `VALIDATION_ERROR`, `AUTH_REQUIRED`, `FORBIDDEN`, `ROLE_MISMATCH`, `RESOURCE_NOT_FOUND`, `CONFLICT`, `CONTRACT_RETENTION_REQUIRED`, `INTERNAL_ERROR`만 사용한다. 인증 뒤 없거나 보이지 않거나 비소유·비당사자·무효 공유인 문서는 모두 404로 존재를 숨기고, 저장소·Checksum·감사 Commit 실패는 500으로 처리한다.                                                                                                                                                                                                                                                                                          | DOC-001, DOC-003~DOC-011                                                       |
| DEC-HEALTH-CERTIFICATE-LIFECYCLE   | 보건증 재등록은 새 문서와 ORIGINAL Version 1을 만들고 기존 문서에 Version을 추가하지 않으며 이전 보건증을 자동 삭제하지 않는다. 발급일은 미래일 수 없고 만료일은 `issuedDate.plusYears(1)`이며 10 MiB 이하 JPG·PNG·PDF의 확장자·MIME·Signature가 모두 일치해야 한다. PATCH는 발급일만 바꾸고 DELETE는 문서 DELETED와 모든 ACTIVE 공유 REVOKED를 한 트랜잭션으로 처리하되 Version·파일·Checksum·감사는 보존한다. 외부 ACTIVE·EXPIRED는 서울 날짜로 계산한다.                                                                                                                                                        | DOC-005, DOC-006, DOC-010                                                      |
| DEC-DOCUMENT-SHARE-UNIT             | 보건증 공유 요청은 `workplaceId`만 받고 서버가 ACTIVE 사업장의 유일한 `ACCEPTED`·`READY` Work Case와 OWNER를 결정한다. 중복 ACTIVE는 409이고 철회 후 재공유는 새 행이다. 접근은 공유·문서·사업장 ACTIVE, 문서 미만료, Work Case `ACCEPTED|READY|IN_PROGRESS`, `now<endsAt`을 매 요청 계산하며 별도 만료 상태 전이는 없다. SHARED 상세의 `workCaseId`는 이 동적 관계를 식별하며 누락·불일치 시 다른 관계로 fallback하지 않는다. 철회는 문서·사업장에 연결된 모든 ACTIVE 공유를 멱등 REVOKED로 바꾼다. | DOC-003, DOC-007, DOC-008, DOC-009                                             |
| DEC-DOCUMENT-ACCESS-AUDIT           | 보건증·계약서 file view/download와 문서 detail은 문서 식별 뒤 허용·거부마다 정확히 한 감사 행을 Commit한다. ALLOWED는 응답 Version ID와 null 사유, DENIED는 식별 가능한 Version ID와 닫힌 사유를 저장한다. SHARED 상세의 누락·불일치 문맥은 `PARTY_ACCESS_DENIED`, 철회·만료·근무 종료는 `DOCUMENT_UNAVAILABLE`이며 외부에는 모두 404다. 감사 Commit 뒤에만 Metadata·Header·Bytes를 반환하며 실패 시 500이다. 목록·공유 변경·미인증·미존재 문서는 DB 감사 대상이 아니고 최소 trace 보안 로그만 남기며 일반 로그에 사용자·파일·Key·Checksum·문서 개인정보를 남기지 않는다. | DOC-003, DOC-009, DOC-011                                                      |
| DEC-TRUST-BADGE-CRITERIA            | OWNER와 WORKER는 누적 10·20·30건과 정상 비율 80·90·100%를 AND로 높은 단계부터 판정한다. OWNER는 COMPLETED Settlement 중 같은 Work Case에 `CANCELED`·`REJECTED`가 아닌 분쟁 행이 없는 건, WORKER는 COMPLETED·NO_SHOW·CHECK_OUT_MISSING과 정시 CHECK_IN을 원천으로 계산한다. 조회마다 사용자 행을 잠근 뒤 Commit된 이력을 재계산해 Member/Auth가 `user_badges`를 Upsert하며 별도 Backfill은 없다. `recentCount`는 누적 건수이고 내 0단계는 객체, 초대 OWNER 0단계는 null이다. evidence에는 `ruleVersion=trust-badge-cumulative-10-20-30-v1`, badgeType·level·totalCount·normalCount·적용 thresholdCount/Percent·calculatedAt만 저장하고 원천 ID와 개인정보는 저장하지 않는다. | BADGE-001, BADGE-002, BADGE-003                                                |
| DEC-DISPUTE-SETTLEMENT              | Work Case OWNER와 배정 WORKER만 1~100자 제목·1~2000자 경위의 `WAGE` 분쟁을 생성·조회하며 열린 분쟁은 한 건만 허용한다. `OPEN`, `UNDER_REVIEW`는 정상 지급과 NO_SHOW 환불을 막고 정상 `SCHEDULED`를 `due_at` 보존 `ON_HOLD`로 바꾼다. 마지막 열린 분쟁이 `RESOLVED`, `REJECTED`, `CANCELED`로 닫히면 정상 정산을 기존 `due_at`의 `SCHEDULED`로 복구하고 NO_SHOW 환불을 다시 허용한다. 분쟁과 지급은 `work_cases→settlements→disputes` 순서로 직렬화하고 지급이 먼저 Commit됐으면 후속 신고가 완료 자금 이동을 되돌리지 않는다. 관리자 상태 변경 권한은 별도 Open 결정으로 남긴다. | DISPUTE-001, DISPUTE-002, DISPUTE-003, SETTLE-002, SETTLE-003, SETTLE-005    |
| DEC-PASSWORD-RESET                  | 비밀번호 재설정은 이메일 식별, Hash 저장, 만료와 1회 사용 Token 방식으로 제공한다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | AUTH-011                                                                       |

## Open

Open 항목은 승인된 경계 밖의 선택지입니다. 결론이 나기 전에는 요구사항이나 API에 임의 값을
추가하지 않습니다.

| ID                                | 결정이 필요한 질문                                                                                                     | 이미 승인된 경계                                                                                             |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| DEC-OPEN-PASSWORD-RESET-DELIVERY  | 비밀번호 재설정 Token 원문을 이메일, 개발용 별도 채널 등 어떤 방식으로 전달할지                                        | API 응답과 일반 로그에는 Token 원문을 노출하지 않고 DB에는 Hash만 저장한다.                                  |
| DEC-OPEN-DASHBOARD-BREAK          | 휴게 분수만 있는 근무에서 무급 휴게를 확보 안심금액의 시간 경과 계산에 어떻게 반영할지                                 | 화면 계산값은 60초마다 갱신되고 0원~합의 일급 범위를 지킨다.                                                 |
| DEC-OPEN-NOTIFICATION-CONTRACT    | 알림 유형, 이벤트 식별자, 목록·읽음 처리 Payload와 전달 방식을 어떻게 정의할지                                         | 수신자만 자신의 알림을 조회하고 읽음 처리하며 동일 이벤트 중복을 막는다.                                     |
| DEC-OPEN-ADMIN-DISPUTE            | 분쟁을 처리할 관리자 역할, 권한과 상태 변경 절차는 무엇인지                                                            | 당사자 생성·조회와 열린 분쟁의 지급·환불 보류, 마지막 열린 분쟁 종료 뒤 정산 재개 계약은 승인됐다.           |
| DEC-OPEN-PAYMENT-PROVIDER         | 외부 결제 Provider, 주문·거래·Webhook 계약과 취소 범위를 어떻게 정할지                                                 | 서버 검증 전에는 지갑을 증가시키지 않고 중복 Webhook을 한 번만 반영한다.                                     |

## Retired

| 폐기 결정                                                                    | 대체 결정                                                         |
| ---------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| JWT 또는 `accessToken`을 로그인 성공 응답으로 반환                           | `DEC-AUTH-SESSION`                                                |
| `/api/auth/check-login-id`, `/api/auth/check-email`                          | `/api/auth/login-id-availability`, `/api/auth/email-availability` |
| `/api/invites/{token}/accept`                                                | `/api/invitations/{token}/accept`                                 |
| 초대 조회 응답에 내부 `termsVersion`을 노출                                  | `DEC-INVITE-LOGIN-BADGE`                                          |
| `/api/shifts/**`, `/api/worker/scan`                                         | `/api/work-cases/**`, `/api/attendance/scans`                     |
| 근무별·동작별 만료 QR                                                        | `DEC-QR-FIXED`                                                    |
| 퇴근 누락을 노쇼로 처리                                                      | `DEC-CHECK-OUT-MISSING`                                           |
| 조기 퇴근 두 번째 스캔을 즉시 기록                                           | `DEC-EARLY-CHECKOUT`                                              |
| 가입 후 로그인 아이디·이메일·이름 수정                                       | `DEC-PROFILE-IMMUTABLE`                                           |
| 사용자가 사업장 인증 반경을 입력·수정                                        | `DEC-WORKPLACE-RADIUS`                                            |
| 대표자와 좌표를 일반 수정 API로 변경                                         | `DEC-WORKPLACE-IMMUTABLE`                                         |
| 일급 전체에 단순 3.3% 공제                                                   | `DEC-DAILY-WORKER-TAX`                                            |
| 사용자가 내부 `bankAccountId`를 선택해 충전·출금                             | `DEC-BANK-INPUT`                                                  |
| 가입 시 사용자 소유 Mock 계좌 자동 발급·사용자 ID 기반 번호·역할별 초기 잔액 | `DEC-MOCK-ACCOUNT-FIXTURE`, `DEC-MOCK-ACCOUNT-SCHEMA`             |
| 사용자용 `GET /api/mock-bank-accounts`와 계좌 등록·연결 API                  | `DEC-MOCK-ACCOUNT-FIXTURE`, `DEC-BANK-INPUT`                      |
| 충전과 출금 모두 인증 사용자 소유 계좌로 판정                                | `DEC-FUNDING-PIN`, `DEC-WITHDRAWAL-DESTINATION`                   |
| 초대 수락 Body에 사용자·OWNER·근무·금액 ID 또는 서명 이미지를 포함           | `DEC-INVITE-ACCEPT`                                               |
| 근로계약서 직접 업로드, 스캔 교체 또는 사용자 삭제                           | `DEC-CONTRACT-AUTO-GENERATION`, `DEC-CONTRACT-RETENTION`          |
| 보건증 재등록 시 기존 문서에 파일 Version을 추가                             | `DEC-HEALTH-CERTIFICATE-LIFECYCLE`                                |
| 신뢰 뱃지를 최근 5·10·15건만으로 판정                                        | `DEC-TRUST-BADGE-CRITERIA`                                       |
| 문서 응답 Shape 및 문서·정산 오류 Catalog를 미결로 유지                      | `DEC-DOCUMENT-RESPONSE-SHAPES`, `DEC-DOCUMENT-ERROR-CATALOG`, `DEC-SETTLEMENT-ERROR-CATALOG` |
| 계약 보존 기준일과 3년 뒤 삭제 범위를 미결로 유지                            | `DEC-CONTRACT-RETENTION`                                         |
| 정산 완료 시점 필드 `settledAt`                                              | `completedAt`                                                     |
| 비인증 `GET /api/auth/session`의 200 또는 401 선택을 미결로 유지             | `DEC-AUTH-SESSION`                                                |
| OWNER 기준정보를 `employer_profiles`에 저장                                  | `DEC-OWNER-ONBOARDING`                                            |
| `employer_profiles.contact_phone`을 `phone`으로 이름 변경                    | `DEC-PHONE-STORAGE`, `DEC-OWNER-ONBOARDING`                       |
| 임금분쟁 신고가 정산 상태·예정 시각과 지급을 바꾸거나 막지 않음              | `DEC-DISPUTE-SETTLEMENT`                                         |
| NO_SHOW 환불의 Settlement 종료 상태를 미결로 유지                            | `DEC-NO-SHOW-SETTLEMENT`                                         |
